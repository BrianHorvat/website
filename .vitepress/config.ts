import { defineConfig } from "vitepress";

// https://vitepress.dev/reference/site-config
export default defineConfig({
  title: "Brian Horvat",
  description: "Brian Horvat portfolio website",
  themeConfig: {},
});
